{
  "name": "ideas-api",
  "version": "0.1.0",
  "author": "Josh Justice <me@codingitwrong.com>",
  "license": "Apache-2.0",
  "scripts": {
    "lint": "prettier --check '{app,lib,spec}/**/*.rb'",
    "format": "prettier --write '{app,lib,spec}/**/*.rb'"
  },
  "devDependencies": {
    "@prettier/plugin-ruby": "^0.12.0",
    "prettier": "^1.17.0"
  }
}
